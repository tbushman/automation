{
  "name": "@freecodecamp/github-tools",
  "description": "Tools to help maintain freecodecamp.org's Open Source Codebase on GitHub",
  "private": true,
  "bin": {
    "presolver": "probot/index.js"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap && npm run build && npm run lint",
    "build": "lerna run build",
    "postinstall": "npm run bootstrap",
    "start": "lerna run start",
    "dev": "nodemon",
    "presolver": "lerna run presolver"
  },
  "devDependencies": {
    "lerna": "^3.5.1",
    "presolver": "file:probot"
  },
  "nodemonConfig": {
    "exec": "npm start",
    "watch": [
      ".env",
      "."
    ]
  },
  "probot": {
    "apps": [
      "presolver"
    ]
  },
  "dependencies": {
    "ajv": "^6.6.1",
    "ajv-keywords": "^3.2.0"
  }
}
